import shell from 'shelljs'
import { project_type } from "./worker";

export 

const cloneAndStartProject = (type: project_type) => {
  switch (type) {
    case "BACKEND":

      break;
    case "FRONTEND":
      break;
    case "EMPTY":
      break;
    default:
      break;
  }
};

export default cloneAndStartProject;
